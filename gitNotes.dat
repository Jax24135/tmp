## Adding new REPOs/commits

git init
git add <file>
git commit -m "first commit"
git remote add origin https://github.com/Jax24135/*.git
git push -u origin master
===
# Enable credential caching:
$ git config credential.helper store
$ git push https://github.com/repo.git
Username for 'https://github.com': <USERNAME>
Password for 'https://USERNAME@github.com': <PASSWORD>

# Set Credential timeout:
$ git config --global credential.helper 'cache --timeout 7200'
After enabling credential caching, it will be cached for 7200 seconds (2 hour).
===
#Fetch git folder from current REPO
git init
git remote add [REMOTE_NAME] [GIT_URL]
git fetch REMOTE_NAME
git checkout REMOTE_NAME/BRANCH -- path/to/directory
===
# Change Git Local/GitHub name/location
$ git remote -v
shows pull/fetch location
$ git remote set-url origin https://github.com/<User>/*.git
# confirm redirect
$ git remote -v
====
# PUSHING simple v matching
$git config --global push.default simple  # pushes only the current branch
$git config --global push.default matching # pushes/merges(?) all branches
